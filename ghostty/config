# Use the explicit path to the system's default shell binary managed by Nix
# AND explicitly point to the config file linked by Home Manager
command = /run/current-system/sw/bin/nu --config ~/.config/nushell/config.nu
theme = GruvboxDarkHard
#adjust-cell-height = 5%
#cursor-style = block
macos-titlebar-style = tabs
mouse-hide-while-typing = true
#window-colorspace = "display-p3"
window-padding-balance = true
window-save-state = always